import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'SSR, SSG, adapters', section: 'App Framework', order: 9 };

export default function SsrSsg() {
  return <SkeletonPage title="SSR, SSG, adapters" />;
}
