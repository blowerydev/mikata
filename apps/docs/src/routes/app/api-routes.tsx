import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'API routes', section: 'App Framework', order: 6 };

export default function ApiRoutes() {
  return <SkeletonPage title="API routes" />;
}
