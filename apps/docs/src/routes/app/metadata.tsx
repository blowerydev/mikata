import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Metadata & head', section: 'App Framework', order: 7 };

export default function Metadata() {
  return <SkeletonPage title="Metadata & head" />;
}
