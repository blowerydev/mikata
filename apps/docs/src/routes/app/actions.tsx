import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Actions & forms', section: 'App Framework', order: 5 };

export default function Actions() {
  return <SkeletonPage title="Actions & forms" />;
}
