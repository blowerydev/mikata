import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Loaders', section: 'App Framework', order: 4 };

export default function Loaders() {
  return <SkeletonPage title="Loaders" />;
}
