import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Deployment', section: 'App Framework', order: 10 };

export default function Deployment() {
  return <SkeletonPage title="Deployment" />;
}
