import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'File routes', section: 'App Framework', order: 2 };

export default function FileRoutes() {
  return <SkeletonPage title="File routes" />;
}
