import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Inputs', section: 'UI', order: 4 };

export default function Inputs() {
  return <SkeletonPage title="Inputs" />;
}
