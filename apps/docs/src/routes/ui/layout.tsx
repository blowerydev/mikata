import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Layout components', section: 'UI', order: 3 };

export default function LayoutComponents() {
  return <SkeletonPage title="Layout components" />;
}
