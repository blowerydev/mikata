import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Feedback', section: 'UI', order: 7 };

export default function Feedback() {
  return <SkeletonPage title="Feedback" />;
}
