import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Overlays', section: 'UI', order: 5 };

export default function Overlays() {
  return <SkeletonPage title="Overlays" />;
}
