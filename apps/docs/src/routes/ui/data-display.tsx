import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Data display', section: 'UI', order: 8 };

export default function DataDisplay() {
  return <SkeletonPage title="Data display" />;
}
