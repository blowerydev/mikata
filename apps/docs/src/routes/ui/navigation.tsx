import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Navigation', section: 'UI', order: 6 };

export default function Navigation() {
  return <SkeletonPage title="Navigation" />;
}
