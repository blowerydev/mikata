import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Theming', section: 'UI', order: 2 };

export default function Theming() {
  return <SkeletonPage title="Theming" />;
}
