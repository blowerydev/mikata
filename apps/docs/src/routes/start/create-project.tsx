import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Create a project', section: 'Start', order: 4 };

export default function CreateProject() {
  return <SkeletonPage title="Create a project" />;
}
