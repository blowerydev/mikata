import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Choosing packages', section: 'Start', order: 7 };

export default function ChoosingPackages() {
  return <SkeletonPage title="Choosing packages" />;
}
