import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Context', section: 'Core Concepts', order: 6 };

export default function Context() {
  return <SkeletonPage title="Context" />;
}
