import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Rendering & hydration', section: 'Core Concepts', order: 4 };

export default function Rendering() {
  return <SkeletonPage title="Rendering & hydration" />;
}
