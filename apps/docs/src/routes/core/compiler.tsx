import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'JSX & compiler', section: 'Core Concepts', order: 3 };

export default function Compiler() {
  return <SkeletonPage title="JSX & compiler" />;
}
