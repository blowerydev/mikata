import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Queries & mutations', section: 'State & Data', order: 2 };

export default function Queries() {
  return <SkeletonPage title="Queries & mutations" />;
}
