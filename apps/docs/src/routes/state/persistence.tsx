import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Persistence', section: 'State & Data', order: 3 };

export default function Persistence() {
  return <SkeletonPage title="Persistence" />;
}
