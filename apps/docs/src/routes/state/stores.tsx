import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Stores', section: 'State & Data', order: 1 };

export default function Stores() {
  return <SkeletonPage title="Stores" />;
}
