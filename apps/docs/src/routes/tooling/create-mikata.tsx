import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'CLI / create-mikata', section: 'Tooling', order: 1 };

export default function CreateMikataTooling() {
  return <SkeletonPage title="CLI / create-mikata" />;
}
