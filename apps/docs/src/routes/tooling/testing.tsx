import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Testing', section: 'Tooling', order: 4 };

export default function TestingTooling() {
  return <SkeletonPage title="Testing" />;
}
