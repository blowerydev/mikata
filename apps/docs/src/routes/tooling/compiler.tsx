import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'Vite compiler', section: 'Tooling', order: 2 };

export default function CompilerTooling() {
  return <SkeletonPage title="Vite compiler" />;
}
