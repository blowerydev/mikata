import { SkeletonPage } from '../../components/SkeletonPage';

export const nav = { title: 'ESLint plugin', section: 'Tooling', order: 3 };

export default function EslintTooling() {
  return <SkeletonPage title="ESLint plugin" />;
}
