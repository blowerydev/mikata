{
  "name": "mikata-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter './packages/*' run build",
    "test": "vitest",
    "test:run": "vitest run",
    "dev": "pnpm --filter playground dev",
    "lint": "eslint packages/*/src/**/*.ts"
  },
  "devDependencies": {
    "@types/babel__core": "^7.20.5",
    "jsdom": "^29.0.2",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
